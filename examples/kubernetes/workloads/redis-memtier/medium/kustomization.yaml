nameSuffix: -medium

bases:
- ../base

commonLabels:
  app: redis-memtier-medium

configMapGenerator:
- name: memtier
  behavior: merge
  literals:
    - keymaximum_gen=500000
    - keymaximum_load=500000
    - extra='--key-stddev=25000'

patchesStrategicMerge:
  - resources.yaml
