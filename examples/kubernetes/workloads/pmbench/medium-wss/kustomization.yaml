apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

nameSuffix: -medium-wss

resources:
- ../base

commonLabels:
  app: pmbench-medium-wss

configMapGenerator:
- name: pmbench
  behavior: merge
  literals:
    - extra='--setsize 40000 --mapsize 40000 2000000 --initialize -a touch -j 1'

patches:
- target:
    kind: StatefulSet
  patch: |-
    apiVersion: foo
    kind: foo
    metadata:
      name: foo
    spec:
      template:
        spec:
          containers:
          - name: generator
            resources:
              requests:
                cpu: 0.1
                memory: 45G
              limits:
                cpu: 0.1 
                memory: 45G
