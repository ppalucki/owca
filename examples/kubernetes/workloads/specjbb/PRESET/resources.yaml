apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: specjbb-group
spec:
  template:
    spec:
      containers:
        - name: backend
          resources:
            requests:
              cpu: 2
              memory: 6G # valueSize*records = 10k * 1M = 10G
