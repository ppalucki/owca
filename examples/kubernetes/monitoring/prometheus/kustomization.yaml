apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: prometheus

resources:
  - prometheus.yaml
  - service_account.yaml
  - service.yaml
  - operator/deployment.yaml
  - operator/service_account.yaml
  - operator/service.yaml
  - storage_class.yaml
  - persistent_volume.yaml

  # RULES - generic/other
  # - prometheusrules.16-compatibility-rules-new-to-old.yaml
  - prometheusrules.apm.yaml
  # - prometheusrules.scheduler.yaml  # e.g. node_type
  - wca
  - cadvisor
  - prometheusrules.score.yaml  # uses data from wca/cadvisor
